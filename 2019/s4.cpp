#include <bits/stdc++.h>

using namespace std;

/**
 * Did not implement
 */

int main() {
    int N, K;
    cin >> N >> K;

    vector<int> scores(N);
    for (int i = 0; i < N; i++) {
        cin >> scores[i];
    }

    return 0;
}